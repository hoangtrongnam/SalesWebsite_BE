namespace Common
{
    public static class Enum
    {
        public enum ConfigKey
        {
            //KeyPath get đường dẫn thư mục lưu file hình ảnh
            KeyPath = 1,
            //KeyPath get đường dẫn thư mục lưu log exception
            PathLogException = 2           
        }
    }
}
